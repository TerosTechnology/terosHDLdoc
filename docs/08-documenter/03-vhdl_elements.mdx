---
description: VHDL elements
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';


import myImageUrl from '../../static/img/documenter/vhdl/process.png';

# VHDL elements
:::note
by default, the documenter will use ```--! ``` to recognize things to document.
:::


## Supported labels
Here are the following labels supported by the documenter:
* Entity description
* Ports             
* Generics          
* Constants         
* Signals           
* Processes         
* Instances         
* Functions         
* Types             

export const Hdl = ({name, children, image,id}) => (
    <div>
    <h2 id={id}> {name} </h2>
    <Tabs>
    <TabItem value="code" label="Code" default>
    <CodeBlock language="vhdl">{children}</CodeBlock>
    </TabItem>
    <TabItem value="result" label="Result">
    <img src={image} alt="Documentation result" />;
    </TabItem>
    </Tabs>
    </div>
);

export const vhdl_elements = [
    {
        'name': "Entity",
        'code': entity,
        'impath' :entityimage
    },
    {
        'name': "Package",
        'code': pkg,
        'impath': pkgimage
    },
    {
        'name': "Generic",
        'code': generic,
        'impath' :genericimage
    },
    {
        'name': "Port",
        'code': port,
        'impath' : portimage
    },
    {
        'name': "Signal",
        'code': signal,
        'impath' : signalimage
    },
    {
        'name': "Constant",
        'code': constant,
        'impath' :constantimage
    },
    {
        'name': "Function",
        'code': f,
        'impath' : fimage
    },
    {
        'name': "Process",
        'code': process,
        'impath' :processimage
    },
    {
        'name': "Type",
        'code': tp,
        'impath' :tpimage
    },
]


import entity from '!!raw-loader!./examples/vhdl/entity.vhd';
import pkg from '!!raw-loader!./examples/vhdl/package.vhd';
import generic from '!!raw-loader!./examples/vhdl/generic.vhd';
import port from '!!raw-loader!./examples/vhdl/port.vhd';
import signal from '!!raw-loader!./examples/vhdl/signal.vhd';
import constant from '!!raw-loader!./examples/vhdl/constant.vhd';
import f from '!!raw-loader!./examples/vhdl/function.vhd';
import process from '!!raw-loader!./examples/vhdl/process.vhd';
import tp from '!!raw-loader!./examples/vhdl/type.vhd';

import entityimage from '@site/static/img/documenter/vhdl/entity.png'
import pkgimage from '@site/static/img/documenter/vhdl/port.png'
import genericimage from '@site/static/img/documenter/vhdl/generic.png'
import portimage from '@site/static/img/documenter/vhdl/port.png'
import signalimage from '@site/static/img/documenter/vhdl/signal.png'
import constantimage from '@site/static/img/documenter/vhdl/constant.png'
import fimage from '@site/static/img/documenter/vhdl/func.png'
import processimage from '@site/static/img/documenter/vhdl/process.png'
import tpimage from '@site/static/img/documenter/vhdl/tp.png'



export const CreateAll = ({elements}) =>(
    <>
{
  elements.map((el) => {
    let newitm = {
        value: el["name"],
        id: el["name"].toLowerCase(),
        level: 3,
    };
    let exists = toc.find((tocid)=>{return tocid["id"]==newitm["id"]})
    if(exists==undefined){
        toc.push(newitm);
    }
    return(
      <Hdl name={el["name"]} image={el["impath"]} children={el["code"]} id={el["name"].toLowerCase()}/>
    )
  })
}
</>
 )


<CreateAll elements={vhdl_elements}/>